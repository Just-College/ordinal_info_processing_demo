[project]
name = "ordinal-info-processing-demo"
version = "0.1.0"
description = "Ordinal sequence processing reproduction"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "librosa>=0.11.0",
]
